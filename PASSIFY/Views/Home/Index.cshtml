@* HOMEPAGE INDEX *@
@model IEnumerable<PASSIFY.Models.Activity>

@{
    ViewData["Title"] = "Home Page";
    var imgWidth = "25px";

}

<div class="text-center p-3">
    <h1 class="text-4xl">Welcome <span class="text-gray-500">@User.Identity.Name</span> to your Passify Dashboard</h1>
</div>

<section class="flex justify-end p-1" id="add-section">
    <div class="flex justify-between">
        @* Add event *@
        <a asp-controller="Activities" asp-action="Create" class="m-2 text-gray-800 no-underline flex flex-col items-center">
            <img src="assets/svg/event.svg" width="@imgWidth" alt="Add Event Icon">
            <span style="">Add event</span>
        </a>
        @* Add Category *@
        <a asp-controller="Categories" asp-action="Create" class="m-2 text-gray-800 no-underline flex flex-col items-center">
            <img src="assets/svg/cat_tag.svg" width="@imgWidth" alt="Add Category Icon"/>
            <span>Add category</span>
        </a>
        <a asp-controller="Organizers" asp-action="Create" class="m-2 text-gray-800 no-underline flex flex-col items-center">
            <img src="assets/svg/organizer.svg" width="@imgWidth" alt="Add Organizer Icon"/>
            <span>Add organizer</span>       
        </a>
    </div>
</section>


@*TODO: convert table to div layout*@
<section class="p-2">
    <table class="w-full border-collapse">
        <thead class="bg-gray-200">
        <tr>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.FormFile)
            </th>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.Title)
            </th>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.EventStart)
            </th>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.EventEnd)
            </th>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.Created)
            </th>
            <th class="p-2 text-left">
                @Html.DisplayNameFor(model => model.Organizer)
            </th>
        </tr>
        </thead>
        
        <tbody class="divide-y divide-gray-200">
            @foreach (var item in Model)
            {
                <tr data-title="@item.Title" data-id="@item.ActivityId" class="hover:bg-gray-100 cursor-pointer align-middle">
                    <td class="flex justify-center p-2">
                        @if(string.IsNullOrWhiteSpace(item.ImageName))
                        {
                            <img src="@Url.Content("~/assets/photos/placeholder.jpg")" alt="" class="img-thumb">
                        }
                        else
                        {
                            <img src="@Url.Content(item.ImageName)" alt="" class="img-thumb">

                        }

                    </td>
                    <td class="p-2">
                        @Html.DisplayFor(modelItem => item.Title)
                    </td>
                    <td class="p-2">
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td class="p-2">
                        @Html.DisplayFor(modelItem => item.EventStart)
                    </td>
                    <td class="p-2">
                        @Html.DisplayFor(modelItem => item.EventEnd)
                    </td>
                    <td class="p-2">
                        @Html.DisplayFor(modelItem => item.Created)
                    </td>
                    <td class="p-2">
                        @Html.DisplayFor(modelItem => item.Organizer.Name)
                    </td>

                </tr>
            }
        </tbody>
    </table>

</section>